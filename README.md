# Python Academy Stuff

## chAItBot
Service which provides several endpoints (APIs) to get a conversation with a bot using, behind the scenes, OpenAI to answer.

## generAItor
Service which provides a way to generate new code based on existent one specifying only the general schema required for the new code. 

## scheduler
Scheduler program to control specific json-configured tasks.  